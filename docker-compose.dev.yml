version: "3"

volumes:
  node_modules:

services:
  redis:
    ports:
      - 6379:6379

  api:
    command: yarn dev:in-docker
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - 4000:4000
      - 9229:9229